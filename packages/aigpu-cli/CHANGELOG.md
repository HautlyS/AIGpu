# Changelog

## 0.2.1 — AIGpu

The CLI now serves bundled docs and a filesystem-backed example catalog without HTTP, cache service, remote discovery, or cloud endpoint. `aigpu mcp` is local stdio only, and `aigpu examples pull` uses an atomic symlink-safe copy.
