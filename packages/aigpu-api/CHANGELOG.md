# Changelog

## 0.3.1 — AIGpu

Added the public `agentAnimation()` API, status/progress/activity uniforms, the built-in agent WGSL shader, and offline CLI packaging.

## 0.3.0 — Upstream-derived runtime

The runtime API, WebGPU adapters, WGSL reflection, scene helpers, and deterministic mock originate from the MIT-licensed upstream code preserved in this fork.
